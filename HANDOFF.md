# 施工效率提升工具：开发交接记录

> 用途：供 ChatGPT、Codex、DeepSeek 或开发人员在切换开发环境时快速恢复项目上下文。
>
> 更新规则：每次暂停开发、切换模型或交付阶段成果前更新本文件。项目代码、测试结果和 Git 状态是事实依据；聊天记录只作补充。
>
> 安全要求：禁止在本文件中记录真实密码、API Key、访问令牌、私钥、身份证号或生产环境敏感数据。

## 1. 交接基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 施工效率提升工具 |
| 项目目录 | `C:\Users\蓝星\Documents\ChatGPT\提高施工效率` |
| 本次交接日期 | `2026-08-16` |
| 交出方 | `DeepSeek` |
| 接收方 | `ChatGPT / Codex / DeepSeek / 开发人员` |
| 当前阶段 | `正式演示版开发与测试` |
| 当前分支 | `master` |
| 当前版本或提交 | `1.0.0 开发基线（本文件所在提交）` |
| 交接状态 | `可继续开发` |

## 2. 一句话说明当前状态

项目已形成可运行的离线演示版和局域网多人版；“每日任务执行中心”已按现场管理习惯重组为今日计划、本周每日完成与实名制考勤、昨日续做、今日跟踪和协调闭环；施工图按单体归档，成控台账包含合同、经济核定单和现场工程量确认单并已增加岗位访问权限；局域网多人版已增加首次登录强制改密、项目经理账号管理（新增/编辑/禁用/重置）以及组织架构与考勤的服务端写权限控制。

## 3. 项目目标与边界

### 3.1 项目目标

- 为项目经理、生产经理和现场管理人员提供施工全过程效率管理工具。
- 统一管理进度、任务、材料设备、工程资料、质量安全、组织架构和考勤。
- 保持离线演示可用，同时逐步完善局域网多人协同和生产部署能力。

### 3.2 本阶段范围

- 维护现有正式演示功能和局域网多人协同能力。
- 新增功能必须通过 `npm test`，并同步更新 README 和本交接文件。

### 3.3 暂不处理

- 未经用户确认，不接入收费 OCR、语音识别、大模型或第三方企业系统。
- 未经用户确认，不进行大规模框架重写、数据库迁移或视觉体系替换。

### 3.4 不可改变的约束

- 材料审批只能由当前节点指定账号操作，其他账号只能查看，不能代办。
- 运行数据库、上传附件、密钥和真实环境配置不得提交到 Git。
- GPT 与 DeepSeek 之间只交接代码、文档、普通文本或稳定 JSON，不交接原始响应对象和内部推理字段。

## 4. 技术与运行环境

| 项目 | 当前配置 |
| --- | --- |
| 前端技术 | `原生 HTML、CSS、JavaScript` |
| 后端技术 | `Node.js 24，原生 HTTP 服务` |
| 数据存储 | `SQLite、浏览器 localStorage/IndexedDB、服务器附件目录` |
| 运行系统 | `Windows 10/11；云部署使用 Docker` |
| 依赖安装命令 | `npm ci` |
| 本地启动命令 | `npm start` 或双击 `启动局域网版.cmd` |
| 测试命令 | `npm test` |
| 默认访问地址 | `http://127.0.0.1:8080` |
| 部署方式 | `离线、局域网、阿里云 Docker；详见 README 和 deploy/aliyun` |

## 5. 已完成功能

| 功能 | 完成情况 | 关键文件 | 验证方式或证据 |
| --- | --- | --- | --- |
| 离线桌面与移动端业务流程 | 已完成 | `index.html`、`app.js`、`styles.css` | `npm test` |
| 局域网登录、共享状态与审批权限 | 已完成 | `server.js`、`server-bridge.js` | `npm test` |
| 材料审批、采购流转与隐蔽验收 | 已完成 | `app.js`、`server.js` | `npm test` |
| 信息采集、人工校核与业务分发 | 已完成 | `index.html`、`app.js`、`styles.css`、`server.js` | `npm run test:intake`、`npm test` |
| 日计划任务联动、日期查询、周计划偏差、班组反馈与资源门禁 | 第一版已完成 | `index.html`、`app.js`、`styles.css` | `npm run test:intake`、桌面/移动端截图 |
| 今日计划、本周每日完成与实名制考勤、昨日遗留和协调闭环 | 已完成 | `app.js`、`styles.css` | `npm run test:intake`、`npm test` |
| 图纸、设计变更、联系函、指令单共享台账 | 已完成 | `index.html`、`app.js`、`server.js`、`server-bridge.js` | 离线与局域网共享测试 |
| 成控文件：合同、经济核定单、现场工程量确认单及岗位权限 | 已完成 | `index.html`、`app.js`、`styles.css`、`server.js`、`server-bridge.js` | 项目经理/商务经理可进入，施工员被前后端拒绝 |
| 阿里云部署资料 | 已完成 | `deploy/aliyun/` | 部署文档和配置检查 |
| 可复现开发基线 | 已完成 | `package.json`、`package-lock.json`、`_test_all.js` | `npm ci`、`npm test` |
| 首次登录强制改密与账号管理 | 已完成 | `server.js`、`app.js`、`index.html`、`styles.css` | 局域网测试：初始密码登录被拦、改密校验、账号创建/禁用/启用/重置 |
| 更完整的服务端授权 | 已完成 | `server.js`、`app.js`、`_test_lan.js` | 未改密禁写、组织架构限项目经理、考勤限劳资员/项目经理，越权 403 + 审计 |

## 6. 当前正在进行的工作

### 6.1 当前任务

每日任务执行中心已根据 19 条页面批注完成重组；当前等待用户在浏览器中确认内容密度、现场称谓和实际业务数据口径。

### 6.2 已做到哪一步

- 已声明并锁定 Playwright、JSZip 开发依赖。
- 已增加一键全量测试、独立测试图片和测试产物忽略规则。
- 新增文件、表格、照片、语音和手工记录入口，所有识别结果必须人工校核后再分发。
- 分发可真实写入任务、计划、材料需求、资料催办、质量问题和现场记录，并保留来源编号。
- 新增共享状态键 `zhuxu-intake-records` 和独立端到端测试 `_test_intake.js`。
- 每日执行反馈使用共享键 `zhuxu-daily-execution`，需协调事项使用 `zhuxu-daily-coordination`；材料与资料直接读取既有业务台账。
- 周进度偏差按所属周计划下的日计划权重汇总：应完成比例取截至查询日应执行的日计划权重，实际比例取这些日计划的执行百分比；低于应完成 5 个百分点以上标记“滞后”。
- 进度计划页的日计划按 `start` 执行日期自动分组归档，日期文件夹显示任务数、完成数和平均完成率，展开后保留每项计划的责任人、责任班组、完成比例和编辑入口。
- 本周每日投入人数严格读取 `zhuxu-attendance`；无打卡记录显示“未上传”。
- 资料条件分为“已满足、待完善、风险项”，复试/检测报告/门禁等关键词进入阻断风险。
- 技术文件使用共享键 `zhuxu-technical-documents`，图纸、变更、联系函和指令单在局域网多账号间同步。
- 成控文件使用共享键 `zhuxu-cost-documents`，合同、经济核定单和现场工程量确认单在局域网多账号间同步。
- 成控文件仅项目经理及岗位名称含“商务、成本、造价”的账号可访问；服务端不向其他岗位下发成控状态，并拒绝其写入和附件下载请求，越权行为写入审计记录。
- 昨日续做只读取上一日实际执行记录并显示“昨日完成百分比”，与今日新计划分区展示；混凝土旁站不再列入昨日续做清单。
- 首次登录强制改密：`users.must_change_password` 标记 + `/api/password/change` 接口；新密码至少 8 位且含字母和数字，改密后其他会话失效；未改密账号的服务端共享数据写入全部被 403 拦截。
- 账号管理：`/api/accounts` 仅项目经理可访问；支持新增（初始密码为手机号后六位）、编辑、禁用/启用（会话立即失效）和重置密码（强制再次改密），全部操作写审计日志并与组织架构状态联动。
- 服务端写权限：`zhuxu-organization` 仅项目经理、`zhuxu-attendance` 仅劳资员或项目经理可写入；前端按钮同步按岗位隐藏，后端越权返回 403 并记录审计。
- 2026-08-16 执行完整 `npm test`，语法、桌面/移动端、材料审批/隐蔽验收、每日执行、技术文件和局域网多人协同全部通过；局域网测试新增首次登录改密、账号创建/禁用/启用/重置与越权拦截验证。

### 6.3 尚未完成

- [ ] 如需提升扫描件、图片和嘈杂语音识别率，由用户确认后再选择企业 OCR、语音服务或大模型接口。
- [ ] 每次功能修改后重新执行 `npm test`。

### 6.4 下一步第一项操作

先执行 `git status`、阅读 `README.md` 与 `HANDOFF.md`，再运行 `npm ci` 和 `npm test`；确认基线通过后才开始下一项功能修改。

## 7. 后续任务优先级

### P0：必须优先处理

- [ ] 当前无已知 P0 阻塞问题。

### P1：本阶段应完成

- [ ] 等待用户确定下一项业务需求。
- [x] 正式生产使用前完善首次登录改密、账号管理和更完整的服务端授权（局域网多人版已实现；生产部署时按企业账号体系对接）。

### P2：后续优化

- [ ] 根据现场试用反馈优化移动端交互和效率分析。

## 8. 关键文件说明

| 文件或目录 | 作用 | 修改注意事项 |
| --- | --- | --- |
| `README.md` | 项目总体说明和运行方法 | 修改运行方式后同步更新 |
| `HANDOFF.md` | 当前开发状态和跨模型交接 | 每次切换模型前更新 |
| `index.html` | 页面结构和业务表单 | 修改元素 ID 时同步更新测试选择器 |
| `styles.css` | 桌面、平板和移动端样式 | 修改后运行全量浏览器测试 |
| `app.js` | 前端状态和业务流程 | 同时兼容离线与局域网模式 |
| `server.js` | SQLite、会话、权限、附件和共享状态 API | 需要 Node.js 24+ |
| `server-bridge.js` | 前端与局域网服务端的连接桥 | 保持离线回退能力 |
| `_test_all.js`、`_test_intake.js` | 一键基线测试及信息采集端到端验证 | 测试不得接触正式数据库 |
| `package.json`、`package-lock.json` | 依赖、命令和版本锁定 | 依赖变更后重新执行 `npm ci` 和 `npm test` |

## 9. 数据、接口与权限

### 9.1 数据位置与备份

- 数据位置：`data/zhuxu-lan.sqlite`，附件位于 `data/uploads/`。
- 是否包含测试数据：当前目录可能包含本机演示数据，但整个 `data/` 已被 Git 忽略。
- 备份方法：停止服务后备份数据库和上传目录；云端使用 `deploy/aliyun/scripts/backup.js`。
- 恢复方法：详见 `deploy/aliyun/README-阿里云部署.md` 和恢复脚本。

### 9.2 外部接口

| 接口或服务 | 用途 | 当前状态 | 配置位置 |
| --- | --- | --- | --- |
| 企业 OCR、语音识别、大模型 | 文件和语音结构化 | 未接入 | 后续按用户选择配置，不在仓库记录密钥 |

### 9.3 权限和业务规则

- 账号与组织架构岗位绑定；材料审批按提报人、生产经理、技术负责人、库管、项目经理顺序执行。
- 局域网模式由服务端校验登录会话、首次登录改密和审批权限；离线模式只用于演示，不强制改密。
- 首次登录改密：新账号和重置后的账号必须修改初始密码（至少 8 位且含字母和数字）；未改密账号不能写任何共享数据、不能审批、不能上传附件。
- 账号管理：`/api/accounts` 的列表、创建、编辑、禁用/启用、重置仅项目经理岗位可用；禁用使会话立即失效，重置恢复初始密码并强制再次改密；操作全部审计。
- 服务端写权限：`zhuxu-organization` 仅项目经理、`zhuxu-attendance` 仅劳资员或项目经理可写入，前端按钮同步隐藏，越权请求 403 并审计。
- 成控文件授权岗位为项目经理及商务、成本、造价岗位；前端入口提示与服务端数据、写入、附件权限共同生效，不能只依赖菜单隐藏。
- 信息采集记录使用共享键 `zhuxu-intake-records`；原始附件沿用现有附件存储，识别候选项未人工确认前不得写入业务台账。

## 10. GPT 与 DeepSeek 交接规则

### 10.1 可以交接

- 项目目录中的代码和文档；
- `README.md`、本文件以及需求、设计和测试说明；
- Git 提交、差异和普通日志；
- 普通文本，或项目自行定义的稳定 JSON 数据格式。

### 10.2 禁止直接交接

- ChatGPT、OpenAI 或 DeepSeek 的完整原始响应对象；
- `reasoning`、`reasoning_content`、`tool_calls`；
- 供应商专用的 `input[]`、`output[]` 或流式事件对象；
- 包含 API Key、密码、令牌或其他敏感信息的内容。

### 10.3 模型间需要传递对话内容时

只转换为普通消息：

```json
{
  "role": "user或assistant",
  "content": "纯文本内容"
}
```

不要把一个模型的内部推理项转换为另一个模型的推理项。

## 11. 已知问题与风险

| 问题或风险 | 影响 | 临时处理 | 推荐解决方案 |
| --- | --- | --- | --- |
| 离线演示密码规则仅用于原型 | 离线模式安全性 | 仅在本机演示使用，不强制改密 | 生产环境使用服务端账号、改密和授权体系（局域网多人版已具备） |
| 浏览器测试默认使用 Windows Chrome 固定路径 | 非标准 Windows 环境或其他系统 | 安装 Chrome 到默认位置 | 后续按部署平台增加浏览器路径自动发现 |
| PDF、图片和语音识别依赖浏览器能力 | 复杂扫描件和嘈杂语音 | 人工校对识别结果 | 按需求接入企业 OCR 和语音服务 |
| 本环境沙箱限制 | 开发便利性 | npm 命令带 `--cache .npm-cache`；`npm test` 需完整权限运行 | 无代码影响 |

## 12. 最近修改记录

| 日期 | 修改文件 | 修改内容 | 是否验证 |
| --- | --- | --- | --- |
| `2026-08-16` | `server.js`、`app.js`、`index.html`、`styles.css`、`_test_lan.js` | 新增首次登录强制改密、项目经理账号管理（新增/编辑/禁用/启用/重置）、组织架构与考勤的服务端写权限及越权审计 | 是，`npm test` 通过 |
| `2026-08-16` | `index.html`、`app.js`、`styles.css`、`_test_intake.js` | 每日执行中心更名每日任务执行中心；协调文案统一为需协调事项；今日计划标题带执行日期；昨日续做标题简化；进度计划日/周计划改为责任人（可多人）+责任班组 | 是 |
| `2026-08-16` | `index.html`、`app.js`、`_test_intake.js` | 每日执行反馈弹窗补充责任人；修正历史进度为逐日递增；日计划新增“当日完成百分比”字段并在列表展示目标/完成 | 是 |
| `2026-08-12` | `.gitignore` | 忽略依赖、npm 缓存、测试截图和运行数据 | 是 |
| `2026-08-12` | `package.json`、`package-lock.json` | 声明并锁定开发依赖与标准命令 | 是，`npm ci` 成功 |
| `2026-08-12` | `_test_all.js` | 增加语法、离线、功能、局域网一键测试 | 是，`npm test` 通过 |
| `2026-08-12` | `_test_app.js`、`_test_new_features.js`、`_test_lan.js` | 内置图片夹具并修复局域网初始化等待 | 是，`npm test` 通过 |
| `2026-08-12` | `README.md`、`HANDOFF.md` | 记录开发基线和跨模型交接方式 | 是 |
| `2026-08-15` | `index.html`、`app.js`、`styles.css`、`server.js`、`server-bridge.js` | 新增信息采集中心、来源追溯、人工校核及六类业务分发 | 是 |
| `2026-08-15` | `_test_intake.js`、`_test_all.js`、`package.json` | 增加信息采集桌面/移动端端到端测试并纳入全量回归 | 是 |
| `2026-08-15` | `index.html`、`app.js`、`styles.css`、`server.js`、`server-bridge.js` | 将信息采集首页升级为每日执行中心，增加班组反馈、线上技术交底、明日协调、材料保障和资料门禁 | 是 |
| `2026-08-15` | `index.html`、`app.js`、`styles.css`、`_test_app.js`、`_test_intake.js`、`_test_lan.js` | 取消总览入口，将每日执行设为首页；今日任务改由日计划生成，增加日期查询、完成率和周计划偏差判断 | 是 |
| `2026-08-16` | `index.html`、`app.js`、`styles.css`、`server.js`、`server-bridge.js` | 重组每日执行首页，增加今日计划、本周完成与考勤、昨日遗留、风险分级、协调状态及四类技术文件台账 | 是 |
| `2026-08-16` | `_test_intake.js`、`_test_lan.js` | 更新 19 条页面批注的端到端验收，并验证技术文件局域网共享 | 是 |
| `2026-08-16` | `index.html`、`app.js`、`styles.css`、`server.js`、`server-bridge.js` | 新增施工图单体文件夹、昨日续做详情、资料风险图标及成控文件台账 | 是 |
| `2026-08-16` | `_test_intake.js`、`_test_lan.js` | 验证施工图目录、三类成控文件及局域网跨账号共享 | 是 |
| `2026-08-16` | `index.html`、`app.js`、`styles.css`、`server.js`、`server-bridge.js`、`_test_lan.js` | 增加成控文件岗位权限、越权弹窗、服务端数据过滤及接口/附件拦截 | 是 |
| `2026-08-16` | `app.js`、`styles.css`、`_test_app.js` | 日计划按执行日期归档为可展开文件夹，并验证任务不会跨日期混放 | 是 |

## 13. 验证记录

### 13.1 已执行检查

| 检查或命令 | 结果 | 日期 |
| --- | --- | --- |
| `npm test` | 通过；更名、日期标题、续做标题及日/周计划责任人+责任班组改动后全量回归通过 | `2026-08-16` |
| `npm test` | 通过；反馈弹窗责任人、历史进度修正、日计划当日完成百分比改动后全量回归通过 | `2026-08-16` |
| `npm ci` | 通过；安装 15 个包，0 个已知漏洞 | `2026-08-12` |
| `npm test` | 通过；7 个语法检查、离线桌面/移动端、材料审批/隐蔽验收、局域网多人协同全部通过 | `2026-08-12` |
| `npm run test:intake` | 通过；验证采集、校核、任务分发、来源追溯及响应式布局 | `2026-08-15` |
| `npm test` | 通过；8 个语法检查、离线桌面/移动端、材料审批/隐蔽验收、信息采集/分发、局域网多人协同全部通过 | `2026-08-15` |
| `npm run test:intake` | 通过；验证今日计划、本周完成与实名制考勤、资料风险分级、协调闭环和技术文件台账 | `2026-08-16` |
| `npm run test:lan` | 通过；验证技术文件、现场信息、任务和材料计划跨账号共享 | `2026-08-16` |
| `npm test` | 通过；8 个语法检查、桌面/移动端、材料审批/隐蔽验收、每日执行和局域网多人协同全部通过 | `2026-08-16` |
| `npm test` | 通过；新增验证项目经理、商务经理可进入成控文件，施工员的页面访问、状态写入均被拒绝 | `2026-08-16` |
| `npm test` | 通过；验证日计划按执行日期归档、展开查看及桌面/移动端响应式布局 | `2026-08-16` |
| `npm test` | 通过；新增验证首次登录强制改密（含错误当前密码/弱密码拦截、未改密禁写）、账号创建/禁用/启用/重置、组织架构与考勤写权限 403 拦截 | `2026-08-16` |

### 13.2 尚未验证

- [ ] 新增业务需求尚未确定，因此没有对应的新验收测试。
- [ ] 正式生产部署仍需在目标服务器、域名和真实终端上验收。
- [ ] 账号管理与改密流程在手机端（响应式布局）的人工操作体验尚未截图验收。

## 14. Git 与工作区状态

- 当前分支：`master`
- 最近提交：`ff8bc83`（feat: enforce first-login password change, account management and server-side write permissions）
- 未提交文件：交接完成时应为“无”；接收方必须以实际 `git status` 为准。
- 新增但未跟踪文件：交接完成时应为“无”；运行数据和测试截图属于已忽略文件。项目目录中的《基于人工智能的建筑工程施工全过程协同管控工法_初稿.md》为用户文档，不属于本工具代码，勿提交或改写。
- 是否已创建交接检查点：`是`
- 特别提醒：禁止使用 `git reset --hard` 或覆盖用户修改；发现非空工作区时先审查差异。

## 15. 待确认事项

只有确实影响开发方向的问题才放在这里：

1. 下一阶段优先开发哪项业务功能，以及对应验收标准是什么？
2. 下一阶段继续以演示版为目标，还是开始按生产环境要求改造？

## 16. 接收方开始工作检查单

- [ ] 已阅读 `README.md` 和 `HANDOFF.md`；
- [ ] 已执行 `git status` 并确认没有覆盖用户现有修改；
- [ ] 已执行 `npm ci`；
- [ ] 已执行 `npm test` 且全部通过；
- [ ] 已找到 `index.html`、`app.js`、`server.js` 等入口；
- [ ] 已核实“下一步第一项操作”；
- [ ] 已先汇报理解，再进行较大范围修改；
- [ ] 已确认不会导入上一模型的原始响应对象。

## 17. 可直接发送给接收模型的开场指令

```text
请继续开发“施工效率提升工具”。

项目目录：
C:\Users\蓝星\Documents\ChatGPT\提高施工效率

请先阅读 README.md 和 HANDOFF.md，并检查当前代码及 Git 状态。以项目文件、测试结果和 Git 差异为事实依据，不要导入上一模型的原始聊天响应对象。

开始修改前，请先向我汇报：
1. 当前已经完成的内容；
2. 正在进行的任务；
3. 已知问题和风险；
4. 你准备执行的下一步。

确认理解无误后，从 HANDOFF.md 中的“下一步第一项操作”继续开发。修改完成后，请同步更新 HANDOFF.md。
```

## 18. 交出方最终确认

- [x] 当前修改已经保存；
- [x] 启动或测试结果已经记录；
- [x] 未在文档中留下敏感信息；
- [x] 已准确记录未完成事项；
- [x] 已写明接收方的第一项操作；
- [x] 已检查 Git 状态；
- [x] 本文件内容与当前代码状态一致。
