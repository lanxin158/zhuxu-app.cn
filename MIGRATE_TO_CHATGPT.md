# 筑序 · 迁移到 ChatGPT 开发的开场说明

> 把本文连同项目源码压缩包一起发给 ChatGPT，作为第一条消息。不要贴 GitHub token、真实密码、`data/` 目录内容或私钥。

---

我准备把「筑序」（施工效率驾驶舱）从本地继续迁移到 ChatGPT 上开发。项目是纯 HTML/CSS/JS + Node.js 24 原生 HTTP + SQLite（`node:sqlite`）的局域网多人协作工具，已发布 v1.1.0（Git tag `v1.1.0`）。

## 请你先做这几件事

1. 阅读随附压缩包里的 `README.md`、`HANDOFF.md`（这是最权威的交接文档）。
2. 以代码、测试结果和 Git 状态为事实依据，不要臆测；没看到的内容就直接说没看到。
3. 先向我复述：项目是什么、已完成什么、还剩什么、你准备先改哪里，再开始动手。

## 技术要点（快速掌握）

- 前端：`index.html`（结构/表单）、`app.js`（约 3900 行，全部业务逻辑与状态）、`styles.css`（样式，基于 CSS 变量 `--blue/--muted/--green/--red` 等）。
- 后端：`server.js`（Node 24，SQLite、会话、权限、附件、共享状态 API、`/api/weather` 代理）；`server-bridge.js`（前端与服务器的连接桥，离线可回退）。
- 数据：SQLite `data/zhuxu-lan.sqlite` + 附件 `data/uploads/`；前端还有 localStorage/IndexedDB（离线演示模式）。
- 运行：`node server.js`（或 `npm start`），访问 `http://127.0.0.1:8080`。
- 测试：`npm test`（Playwright 需要本机装 Chrome）；依赖 `npm ci`（若报缓存权限，加 `--cache .npm-cache`）。
- 环境要求：Node.js **24+**（因用了 `node:sqlite`）。

## 已完成（v1.1.0）

按用户 17 项改进清单全部落地：左侧菜单精简与顺序调整、计划表原文件预览、计划撤销、计划多子任务自动拆分到每日执行、全量文件拖拽+在线预览、组织架构仅保留项目管理人员、独立「民工管理」模块（花名册 + 考勤 Excel 按姓名自动匹配）、质量整改确认审计、材料计划撤回补传、登记资源去重、资料台账自动生成并导出、技术交底登记变更/指令单、天气自动记录与晴雨表（Open-Meteo，按项目设置所在地）。

此前已具备：离线演示 + 局域网多人协同、多项目/多账号、首次登录强制改密、账号管理、服务端写权限、材料审批流转、隐蔽验收、信息采集校核分发、成控文件岗位权限。

## 我的约束（务必遵守）

- 不要提交 `data/`、`.npm-cache/`、`node_modules/`、`.git/`、`_inspect_ui.js`，以及《基于人工智能的建筑工程施工全过程协同管控工法_初稿.md》（用户文档）。
- 不要引入收费 OCR/语音/大模型接口（除非用户明确同意）；不接入第三方企业系统。
- 不要做大框架重写、数据库迁移或视觉体系替换（除非用户确认）。
- 每改完一轮，告诉我改了什么、怎么在本机验证（`npm test`），并把改动结果贴回来，我在本机跑测试后再合并。
