# 生产环境建议都设置为 1
# 注意：HTTPS 场景下必须为 1；若临时用明文 http://IP:8080 测试，请改为 0
ZHUXU_COOKIE_SECURE=1
ZHUXU_TRUST_PROXY=1
# JSON 请求体上限（字节），默认 12MB
ZHUXU_MAX_BODY=12582912
# 单个附件上限（字节），默认 25MB
ZHUXU_MAX_ATTACHMENT=26214400